import type { Preview } from '@storybook/react';

// TODO: merge changes from master where all stories are migrated to CSF

export default {
  parameters: { layout: 'none', mode: 'vr-test' },
} satisfies Preview;
