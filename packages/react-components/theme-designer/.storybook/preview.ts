import type { Preview } from '@storybook/react';
import rootPreview from '../../../../.storybook/preview';

import './docs-root-theme-designer.css';

export default { ...rootPreview } satisfies Preview;
