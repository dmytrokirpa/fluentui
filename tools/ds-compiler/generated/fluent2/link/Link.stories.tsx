/**
 * Generated by @fluentui/ds-compiler — do not edit by hand.
 */
import type { Meta, StoryObj } from '@storybook/react';
import { Link } from './index';

const meta: Meta<typeof Link> = {
  title: 'Fluent2 CSS/Link',
  component: Link,
  args: {
    appearance: 'default',
    inline: 'false',
    children: 'Link',
  },
  argTypes: {
    appearance: { control: 'select', options: ['default', 'subtle'] },
    inline: { control: 'select', options: ['false', 'true'] },
  },
};

export default meta;
type Story = StoryObj<typeof Link>;

export const Default: Story = {};

export const Disabled: Story = {
  args: { disabled: true },
};

export const DisabledFocusable: Story = {
  args: { disabled: true, disabledFocusable: true },
};

export const Matrix: Story = {
  render: () => (
    <div style={{ display: 'grid', gap: 12 }}>
      {(['default', 'subtle'] as const).map(appearance => (
        <div key={appearance} style={{ display: 'flex', gap: 8, alignItems: 'center', flexWrap: 'wrap' }}>
          <strong style={{ width: 100 }}>appearance={appearance}</strong>
          {(['false', 'true'] as const).map(inline => (
            <Link key={inline} appearance={appearance} inline={inline}>
              {inline}
            </Link>
          ))}
          <Link appearance={appearance} disabled>
            disabled
          </Link>
        </div>
      ))}
    </div>
  ),
};
