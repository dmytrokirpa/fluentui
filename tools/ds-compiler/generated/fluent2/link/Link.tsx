/**
 * Generated by @fluentui/ds-compiler — do not edit by hand.
 * Recipe: Link (react-link/useLinkStyles.styles.ts)
 */
'use client';

import * as React from 'react';
import { useLink as useHeadless, renderLink as renderHeadless } from '@fluentui/react-headless-components-preview/link';
import type {
  LinkProps as HeadlessProps,
  LinkState as HeadlessState,
} from '@fluentui/react-headless-components-preview/link';

export type LinkVariants = {
  appearance?: 'default' | 'subtle';
  inline?: 'false' | 'true';
};

export type LinkProps = HeadlessProps & LinkVariants;

export type LinkState = HeadlessState & Required<Pick<LinkVariants, 'appearance' | 'inline'>>;

function useLinkStyles(state: LinkState, variants: LinkVariants): void {
  const appearance = variants.appearance ?? state.appearance;
  const inline = variants.inline ?? state.inline;

  if (appearance !== undefined) {
    (state.root as Record<string, unknown>)['data-fui-appearance'] = appearance;
  }
  if (inline !== undefined) {
    (state.root as Record<string, unknown>)['data-fui-inline'] = inline;
  }

  state.root.className = [state.root.className, 'fui-Link'].filter(Boolean).join(' ');
}

/**
 * Fluent 2 styled Link built on the headless primitive.
 */
export const Link = React.forwardRef(function Link(
  props: LinkProps,
  ref: React.ForwardedRef<HTMLButtonElement | HTMLAnchorElement>,
) {
  const { appearance = 'default', inline = 'false', ...rest } = props;

  const state = useHeadless(rest, ref) as LinkState;
  state.appearance = appearance ?? 'default'!;
  state.inline = inline ?? 'false'!;

  useLinkStyles(state, { appearance, inline });

  return renderHeadless(state);
});

Link.displayName = 'Link';
