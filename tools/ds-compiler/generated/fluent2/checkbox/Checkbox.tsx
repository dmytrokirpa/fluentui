/**
 * Generated by @fluentui/ds-compiler — do not edit by hand.
 * Recipe: Checkbox (react-checkbox/useCheckboxStyles.styles.ts)
 */
'use client';

import * as React from 'react';
import {
  useCheckbox as useHeadless,
  renderCheckbox as renderHeadless,
} from '@fluentui/react-headless-components-preview/checkbox';
import type {
  CheckboxProps as HeadlessProps,
  CheckboxState as HeadlessState,
} from '@fluentui/react-headless-components-preview/checkbox';

export type CheckboxVariants = {
  shape?: 'square' | 'circular';
  size?: 'medium' | 'large';
};

export type CheckboxProps = HeadlessProps & CheckboxVariants;

export type CheckboxState = HeadlessState & Required<Pick<CheckboxVariants, 'shape' | 'size'>>;

function useCheckboxStyles(state: CheckboxState, variants: CheckboxVariants): void {
  const shape = variants.shape ?? state.shape;
  const size = variants.size ?? state.size;

  if (shape !== undefined) {
    (state.root as Record<string, unknown>)['data-fui-shape'] = shape;
  }
  if (size !== undefined) {
    (state.root as Record<string, unknown>)['data-fui-size'] = size;
  }

  state.root.className = [state.root.className, 'fui-Checkbox'].filter(Boolean).join(' ');
  if (state.label) {
    state.label.className = [state.label.className, 'fui-Checkbox__label'].filter(Boolean).join(' ');
  }
  if (state.input) {
    state.input.className = [state.input.className, 'fui-Checkbox__input'].filter(Boolean).join(' ');
  }
  if (state.indicator) {
    state.indicator.className = [state.indicator.className, 'fui-Checkbox__indicator'].filter(Boolean).join(' ');
  }
}

/**
 * Fluent 2 styled Checkbox built on the headless primitive.
 */
export const Checkbox = React.forwardRef(function Checkbox(
  props: CheckboxProps,
  ref: React.ForwardedRef<HTMLButtonElement | HTMLAnchorElement>,
) {
  const { shape = 'square', size = 'medium', ...rest } = props;

  const state = useHeadless(rest, ref) as CheckboxState;
  state.shape = shape ?? 'square'!;
  state.size = size ?? 'medium'!;

  useCheckboxStyles(state, { shape, size });

  return renderHeadless(state);
});

Checkbox.displayName = 'Checkbox';
