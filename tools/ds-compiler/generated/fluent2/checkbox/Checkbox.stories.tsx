/**
 * Generated by @fluentui/ds-compiler — do not edit by hand.
 */
import type { Meta, StoryObj } from '@storybook/react';
import { Checkbox } from './index';

const meta: Meta<typeof Checkbox> = {
  title: 'Fluent2 CSS/Checkbox',
  component: Checkbox,
  args: {
    shape: 'square',
    size: 'medium',
    children: 'Checkbox',
  },
  argTypes: {
    shape: { control: 'select', options: ['square', 'circular'] },
    size: { control: 'select', options: ['medium', 'large'] },
  },
};

export default meta;
type Story = StoryObj<typeof Checkbox>;

export const Default: Story = {};

export const Disabled: Story = {
  args: { disabled: true },
};

export const DisabledFocusable: Story = {
  args: { disabled: true, disabledFocusable: true },
};

export const Matrix: Story = {
  render: () => (
    <div style={{ display: 'grid', gap: 12 }}>
      {(['square', 'circular'] as const).map(shape => (
        <div key={shape} style={{ display: 'flex', gap: 8, alignItems: 'center', flexWrap: 'wrap' }}>
          <strong style={{ width: 100 }}>shape={shape}</strong>
          {(['medium', 'large'] as const).map(size => (
            <Checkbox key={size} shape={shape} size={size}>
              {size}
            </Checkbox>
          ))}
          <Checkbox shape={shape} disabled>
            disabled
          </Checkbox>
        </div>
      ))}
    </div>
  ),
};
