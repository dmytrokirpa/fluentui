/**
 * Generated by @fluentui/ds-compiler — do not edit by hand.
 * Recipe: Slider (react-slider/useSliderStyles.styles.ts)
 */
'use client';

import * as React from 'react';
import {
  useSlider as useHeadless,
  renderSlider as renderHeadless,
} from '@fluentui/react-headless-components-preview/slider';
import type {
  SliderProps as HeadlessProps,
  SliderState as HeadlessState,
} from '@fluentui/react-headless-components-preview/slider';

export type SliderVariants = {
  size?: 'small' | 'medium';
};

export type SliderProps = HeadlessProps & SliderVariants;

export type SliderState = HeadlessState & Required<Pick<SliderVariants, 'size'>>;

function useSliderStyles(state: SliderState, variants: SliderVariants): void {
  const size = variants.size ?? state.size;

  if (size !== undefined) {
    (state.root as Record<string, unknown>)['data-fui-size'] = size;
  }

  state.root.className = [state.root.className, 'fui-Slider'].filter(Boolean).join(' ');
  if (state.rail) {
    state.rail.className = [state.rail.className, 'fui-Slider__rail'].filter(Boolean).join(' ');
  }
  if (state.thumb) {
    state.thumb.className = [state.thumb.className, 'fui-Slider__thumb'].filter(Boolean).join(' ');
  }
  if (state.input) {
    state.input.className = [state.input.className, 'fui-Slider__input'].filter(Boolean).join(' ');
  }
}

/**
 * Fluent 2 styled Slider built on the headless primitive.
 */
export const Slider = React.forwardRef(function Slider(
  props: SliderProps,
  ref: React.ForwardedRef<HTMLButtonElement | HTMLAnchorElement>,
) {
  const { size = 'medium', ...rest } = props;

  const state = useHeadless(rest, ref) as SliderState;
  state.size = size ?? 'medium'!;

  useSliderStyles(state, { size });

  return renderHeadless(state);
});

Slider.displayName = 'Slider';
