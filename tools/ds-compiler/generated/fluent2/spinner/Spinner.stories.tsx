/**
 * Generated by @fluentui/ds-compiler — do not edit by hand.
 */
import type { Meta, StoryObj } from '@storybook/react';
import { Spinner } from './index';

const meta: Meta<typeof Spinner> = {
  title: 'Fluent2 CSS/Spinner',
  component: Spinner,
  args: {
    appearance: 'primary',
    size: 'medium',
    children: 'Spinner',
  },
  argTypes: {
    appearance: { control: 'select', options: ['primary', 'inverted'] },
    size: {
      control: 'select',
      options: ['extra-tiny', 'tiny', 'extra-small', 'small', 'medium', 'large', 'extra-large', 'huge'],
    },
  },
};

export default meta;
type Story = StoryObj<typeof Spinner>;

export const Default: Story = {};

export const Disabled: Story = {
  args: { disabled: true },
};

export const DisabledFocusable: Story = {
  args: { disabled: true, disabledFocusable: true },
};

export const Matrix: Story = {
  render: () => (
    <div style={{ display: 'grid', gap: 12 }}>
      {(['primary', 'inverted'] as const).map(appearance => (
        <div key={appearance} style={{ display: 'flex', gap: 8, alignItems: 'center', flexWrap: 'wrap' }}>
          <strong style={{ width: 100 }}>appearance={appearance}</strong>
          {(['extra-tiny', 'tiny', 'extra-small', 'small', 'medium', 'large', 'extra-large', 'huge'] as const).map(
            size => (
              <Spinner key={size} appearance={appearance} size={size}>
                {size}
              </Spinner>
            ),
          )}
          <Spinner appearance={appearance} disabled>
            disabled
          </Spinner>
        </div>
      ))}
    </div>
  ),
};
