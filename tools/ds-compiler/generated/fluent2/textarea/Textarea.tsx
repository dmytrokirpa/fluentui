/**
 * Generated by @fluentui/ds-compiler — do not edit by hand.
 * Recipe: Textarea (react-textarea/useTextareaStyles.styles.ts)
 */
'use client';

import * as React from 'react';
import {
  useTextarea as useHeadless,
  renderTextarea as renderHeadless,
} from '@fluentui/react-headless-components-preview/textarea';
import type {
  TextareaProps as HeadlessProps,
  TextareaState as HeadlessState,
} from '@fluentui/react-headless-components-preview/textarea';

export type TextareaVariants = {
  appearance?: 'outline' | 'filled-darker' | 'filled-lighter';
  size?: 'small' | 'medium' | 'large';
};

export type TextareaProps = HeadlessProps & TextareaVariants;

export type TextareaState = HeadlessState & Required<Pick<TextareaVariants, 'appearance' | 'size'>>;

function useTextareaStyles(state: TextareaState, variants: TextareaVariants): void {
  const appearance = variants.appearance ?? state.appearance;
  const size = variants.size ?? state.size;

  if (appearance !== undefined) {
    (state.root as Record<string, unknown>)['data-fui-appearance'] = appearance;
  }
  if (size !== undefined) {
    (state.root as Record<string, unknown>)['data-fui-size'] = size;
  }

  state.root.className = [state.root.className, 'fui-Textarea'].filter(Boolean).join(' ');
  if (state.textarea) {
    state.textarea.className = [state.textarea.className, 'fui-Textarea__textarea'].filter(Boolean).join(' ');
  }
}

/**
 * Fluent 2 styled Textarea built on the headless primitive.
 */
export const Textarea = React.forwardRef(function Textarea(
  props: TextareaProps,
  ref: React.ForwardedRef<HTMLButtonElement | HTMLAnchorElement>,
) {
  const { appearance = 'outline', size = 'medium', ...rest } = props;

  const state = useHeadless(rest, ref) as TextareaState;
  state.appearance = appearance ?? 'outline'!;
  state.size = size ?? 'medium'!;

  useTextareaStyles(state, { appearance, size });

  return renderHeadless(state);
});

Textarea.displayName = 'Textarea';
