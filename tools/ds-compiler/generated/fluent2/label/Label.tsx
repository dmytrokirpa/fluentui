/**
 * Generated by @fluentui/ds-compiler — do not edit by hand.
 * Recipe: Label (react-label/useLabelStyles.styles.ts)
 */
'use client';

import * as React from 'react';
import {
  useLabel as useHeadless,
  renderLabel as renderHeadless,
} from '@fluentui/react-headless-components-preview/label';
import type {
  LabelProps as HeadlessProps,
  LabelState as HeadlessState,
} from '@fluentui/react-headless-components-preview/label';

export type LabelVariants = {
  size?: 'small' | 'medium' | 'large';
  weight?: 'regular' | 'semibold';
};

export type LabelProps = HeadlessProps & LabelVariants;

export type LabelState = HeadlessState & Required<Pick<LabelVariants, 'size' | 'weight'>>;

function useLabelStyles(state: LabelState, variants: LabelVariants): void {
  const size = variants.size ?? state.size;
  const weight = variants.weight ?? state.weight;

  if (size !== undefined) {
    (state.root as Record<string, unknown>)['data-fui-size'] = size;
  }
  if (weight !== undefined) {
    (state.root as Record<string, unknown>)['data-fui-weight'] = weight;
  }

  state.root.className = [state.root.className, 'fui-Label'].filter(Boolean).join(' ');
  if (state.required) {
    state.required.className = [state.required.className, 'fui-Label__required'].filter(Boolean).join(' ');
  }
}

/**
 * Fluent 2 styled Label built on the headless primitive.
 */
export const Label = React.forwardRef(function Label(
  props: LabelProps,
  ref: React.ForwardedRef<HTMLButtonElement | HTMLAnchorElement>,
) {
  const { size = 'medium', weight = 'regular', ...rest } = props;

  const state = useHeadless(rest, ref) as LabelState;
  state.size = size ?? 'medium'!;
  state.weight = weight ?? 'regular'!;

  useLabelStyles(state, { size, weight });

  return renderHeadless(state);
});

Label.displayName = 'Label';
