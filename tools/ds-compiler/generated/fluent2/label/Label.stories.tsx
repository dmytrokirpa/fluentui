/**
 * Generated by @fluentui/ds-compiler — do not edit by hand.
 */
import type { Meta, StoryObj } from '@storybook/react';
import { Label } from './index';

const meta: Meta<typeof Label> = {
  title: 'Fluent2 CSS/Label',
  component: Label,
  args: {
    size: 'medium',
    weight: 'regular',
    children: 'Label',
  },
  argTypes: {
    size: { control: 'select', options: ['small', 'medium', 'large'] },
    weight: { control: 'select', options: ['regular', 'semibold'] },
  },
};

export default meta;
type Story = StoryObj<typeof Label>;

export const Default: Story = {};

export const Disabled: Story = {
  args: { disabled: true },
};

export const DisabledFocusable: Story = {
  args: { disabled: true, disabledFocusable: true },
};

export const Matrix: Story = {
  render: () => (
    <div style={{ display: 'grid', gap: 12 }}>
      {(['small', 'medium', 'large'] as const).map(size => (
        <div key={size} style={{ display: 'flex', gap: 8, alignItems: 'center', flexWrap: 'wrap' }}>
          <strong style={{ width: 100 }}>size={size}</strong>
          {(['regular', 'semibold'] as const).map(weight => (
            <Label key={weight} size={size} weight={weight}>
              {weight}
            </Label>
          ))}
          <Label size={size} disabled>
            disabled
          </Label>
        </div>
      ))}
    </div>
  ),
};
