/**
 * Generated by @fluentui/ds-compiler — do not edit by hand.
 */
import type { Meta, StoryObj } from '@storybook/react';
import { ToggleButton } from './index';

const meta: Meta<typeof ToggleButton> = {
  title: 'Fluent2 CSS/ToggleButton',
  component: ToggleButton,
  args: {
    appearance: 'secondary',
    size: 'medium',
    shape: 'rounded',
    children: 'ToggleButton',
  },
  argTypes: {
    appearance: { control: 'select', options: ['secondary', 'primary', 'outline', 'subtle', 'transparent'] },
    size: { control: 'select', options: ['small', 'medium', 'large'] },
    shape: { control: 'select', options: ['rounded', 'circular', 'square'] },
  },
};

export default meta;
type Story = StoryObj<typeof ToggleButton>;

export const Default: Story = {};

export const Disabled: Story = {
  args: { disabled: true },
};

export const DisabledFocusable: Story = {
  args: { disabled: true, disabledFocusable: true },
};

export const Matrix: Story = {
  render: () => (
    <div style={{ display: 'grid', gap: 12 }}>
      {(['secondary', 'primary', 'outline', 'subtle', 'transparent'] as const).map(appearance => (
        <div key={appearance} style={{ display: 'flex', gap: 8, alignItems: 'center', flexWrap: 'wrap' }}>
          <strong style={{ width: 100 }}>appearance={appearance}</strong>
          {(['small', 'medium', 'large'] as const).map(size => (
            <ToggleButton key={size} appearance={appearance} size={size}>
              {size}
            </ToggleButton>
          ))}
          <ToggleButton appearance={appearance} disabled>
            disabled
          </ToggleButton>
        </div>
      ))}
    </div>
  ),
};
