/**
 * Generated by @fluentui/ds-compiler — do not edit by hand.
 * Recipe: ToggleButton (react-button/useToggleButtonStyles.styles.ts)
 */
'use client';

import * as React from 'react';
import {
  useToggleButton as useHeadless,
  renderToggleButton as renderHeadless,
} from '@fluentui/react-headless-components-preview/toggle-button';
import type {
  ToggleButtonProps as HeadlessProps,
  ToggleButtonState as HeadlessState,
} from '@fluentui/react-headless-components-preview/toggle-button';

export type ToggleButtonVariants = {
  appearance?: 'secondary' | 'primary' | 'outline' | 'subtle' | 'transparent';
  size?: 'small' | 'medium' | 'large';
  shape?: 'rounded' | 'circular' | 'square';
};

export type ToggleButtonProps = HeadlessProps & ToggleButtonVariants;

export type ToggleButtonState = HeadlessState & Required<Pick<ToggleButtonVariants, 'appearance' | 'size' | 'shape'>>;

function useToggleButtonStyles(state: ToggleButtonState, variants: ToggleButtonVariants): void {
  const appearance = variants.appearance ?? state.appearance;
  const size = variants.size ?? state.size;
  const shape = variants.shape ?? state.shape;

  if (appearance !== undefined) {
    (state.root as Record<string, unknown>)['data-fui-appearance'] = appearance;
  }
  if (size !== undefined) {
    (state.root as Record<string, unknown>)['data-fui-size'] = size;
  }
  if (shape !== undefined) {
    (state.root as Record<string, unknown>)['data-fui-shape'] = shape;
  }

  state.root.className = [state.root.className, 'fui-ToggleButton'].filter(Boolean).join(' ');
  if (state.icon) {
    state.icon.className = [state.icon.className, 'fui-ToggleButton__icon'].filter(Boolean).join(' ');
  }
}

/**
 * Fluent 2 styled ToggleButton built on the headless primitive.
 */
export const ToggleButton = React.forwardRef(function ToggleButton(
  props: ToggleButtonProps,
  ref: React.ForwardedRef<HTMLButtonElement | HTMLAnchorElement>,
) {
  const { appearance = 'secondary', size = 'medium', shape = 'rounded', ...rest } = props;

  const state = useHeadless(rest, ref) as ToggleButtonState;
  state.appearance = appearance ?? 'secondary'!;
  state.size = size ?? 'medium'!;
  state.shape = shape ?? 'rounded'!;

  useToggleButtonStyles(state, { appearance, size, shape });

  return renderHeadless(state);
});

ToggleButton.displayName = 'ToggleButton';
