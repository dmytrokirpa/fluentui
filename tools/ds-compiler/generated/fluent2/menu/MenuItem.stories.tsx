/**
 * Generated by @fluentui/ds-compiler — do not edit by hand.
 */
import type { Meta, StoryObj } from '@storybook/react';
import { MenuItem } from './index';

const meta: Meta<typeof MenuItem> = {
  title: 'Fluent2 CSS/MenuItem',
  component: MenuItem,
  args: {
    children: 'MenuItem',
  },
  argTypes: {},
};

export default meta;
type Story = StoryObj<typeof MenuItem>;

export const Default: Story = {};

export const Disabled: Story = {
  args: { disabled: true },
};

export const DisabledFocusable: Story = {
  args: { disabled: true, disabledFocusable: true },
};

export const Matrix: Story = {
  render: () => (
    <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
      {(['secondary'] as const).map(v => (
        <MenuItem key={v} appearance={v}>
          {v}
        </MenuItem>
      ))}
    </div>
  ),
};
