/**
 * Generated by @fluentui/ds-compiler — do not edit by hand.
 * Recipe: MenuDivider (react-menu/useMenuDividerStyles.styles.ts)
 */
'use client';

import * as React from 'react';
import {
  useMenuDivider as useHeadless,
  renderMenuDivider as renderHeadless,
} from '@fluentui/react-headless-components-preview/menu';
import type {
  MenuDividerProps as HeadlessProps,
  MenuDividerState as HeadlessState,
} from '@fluentui/react-headless-components-preview/menu';

export type MenuDividerVariants = {};

export type MenuDividerProps = HeadlessProps & MenuDividerVariants;

export type MenuDividerState = HeadlessState & Required<Pick<MenuDividerVariants, never>>;

function useMenuDividerStyles(state: MenuDividerState, variants: MenuDividerVariants): void {
  // no variants

  // no variant attrs

  state.root.className = [state.root.className, 'fui-MenuDivider'].filter(Boolean).join(' ');
}

/**
 * Fluent 2 styled MenuDivider built on the headless primitive.
 */
export const MenuDivider = React.forwardRef(function MenuDivider(
  props: MenuDividerProps,
  ref: React.ForwardedRef<HTMLButtonElement | HTMLAnchorElement>,
) {
  const {
    // no variants
    ...rest
  } = props;

  const state = useHeadless(rest, ref) as MenuDividerState;

  useMenuDividerStyles(state, {});

  return renderHeadless(state);
});

MenuDivider.displayName = 'MenuDivider';
