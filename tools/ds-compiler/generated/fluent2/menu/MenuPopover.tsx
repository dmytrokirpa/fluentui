/**
 * Generated by @fluentui/ds-compiler — do not edit by hand.
 * Recipe: MenuPopover (react-menu/useMenuPopoverStyles.styles.ts)
 */
'use client';

import * as React from 'react';
import {
  useMenuPopover as useHeadless,
  renderMenuPopover as renderHeadless,
} from '@fluentui/react-headless-components-preview/menu';
import type {
  MenuPopoverProps as HeadlessProps,
  MenuPopoverState as HeadlessState,
} from '@fluentui/react-headless-components-preview/menu';

export type MenuPopoverVariants = {};

export type MenuPopoverProps = HeadlessProps & MenuPopoverVariants;

export type MenuPopoverState = HeadlessState & Required<Pick<MenuPopoverVariants, never>>;

function useMenuPopoverStyles(state: MenuPopoverState, variants: MenuPopoverVariants): void {
  // no variants

  // no variant attrs

  state.root.className = [state.root.className, 'fui-MenuPopover'].filter(Boolean).join(' ');
}

/**
 * Fluent 2 styled MenuPopover built on the headless primitive.
 */
export const MenuPopover = React.forwardRef(function MenuPopover(
  props: MenuPopoverProps,
  ref: React.ForwardedRef<HTMLButtonElement | HTMLAnchorElement>,
) {
  const {
    // no variants
    ...rest
  } = props;

  const state = useHeadless(rest, ref) as MenuPopoverState;

  useMenuPopoverStyles(state, {});

  return renderHeadless(state);
});

MenuPopover.displayName = 'MenuPopover';
