/**
 * Generated by @fluentui/ds-compiler — do not edit by hand.
 * Recipe: MenuGroupHeader (react-menu/useMenuGroupHeaderStyles.styles.ts)
 */
'use client';

import * as React from 'react';
import {
  useMenuGroupHeader as useHeadless,
  renderMenuGroupHeader as renderHeadless,
} from '@fluentui/react-headless-components-preview/menu';
import type {
  MenuGroupHeaderProps as HeadlessProps,
  MenuGroupHeaderState as HeadlessState,
} from '@fluentui/react-headless-components-preview/menu';

export type MenuGroupHeaderVariants = {};

export type MenuGroupHeaderProps = HeadlessProps & MenuGroupHeaderVariants;

export type MenuGroupHeaderState = HeadlessState & Required<Pick<MenuGroupHeaderVariants, never>>;

function useMenuGroupHeaderStyles(state: MenuGroupHeaderState, variants: MenuGroupHeaderVariants): void {
  // no variants

  // no variant attrs

  state.root.className = [state.root.className, 'fui-MenuGroupHeader'].filter(Boolean).join(' ');
}

/**
 * Fluent 2 styled MenuGroupHeader built on the headless primitive.
 */
export const MenuGroupHeader = React.forwardRef(function MenuGroupHeader(
  props: MenuGroupHeaderProps,
  ref: React.ForwardedRef<HTMLButtonElement | HTMLAnchorElement>,
) {
  const {
    // no variants
    ...rest
  } = props;

  const state = useHeadless(rest, ref) as MenuGroupHeaderState;

  useMenuGroupHeaderStyles(state, {});

  return renderHeadless(state);
});

MenuGroupHeader.displayName = 'MenuGroupHeader';
