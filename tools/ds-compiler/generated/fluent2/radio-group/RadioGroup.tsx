/**
 * Generated by @fluentui/ds-compiler — do not edit by hand.
 * Recipe: RadioGroup (react-radio/useRadioGroupStyles.styles.ts)
 */
'use client';

import * as React from 'react';
import {
  useRadioGroup as useHeadless,
  renderRadioGroup as renderHeadless,
} from '@fluentui/react-headless-components-preview/radio-group';
import type {
  RadioGroupProps as HeadlessProps,
  RadioGroupState as HeadlessState,
} from '@fluentui/react-headless-components-preview/radio-group';

export type RadioGroupVariants = {
  layout?: 'vertical' | 'horizontal' | 'horizontal-stacked';
};

export type RadioGroupProps = HeadlessProps & RadioGroupVariants;

export type RadioGroupState = HeadlessState & Required<Pick<RadioGroupVariants, 'layout'>>;

function useRadioGroupStyles(state: RadioGroupState, variants: RadioGroupVariants): void {
  const layout = variants.layout ?? state.layout;

  if (layout !== undefined) {
    (state.root as Record<string, unknown>)['data-fui-layout'] = layout;
  }

  state.root.className = [state.root.className, 'fui-RadioGroup'].filter(Boolean).join(' ');
}

/**
 * Fluent 2 styled RadioGroup built on the headless primitive.
 */
export const RadioGroup = React.forwardRef(function RadioGroup(
  props: RadioGroupProps,
  ref: React.ForwardedRef<HTMLButtonElement | HTMLAnchorElement>,
) {
  const { layout = 'vertical', ...rest } = props;

  const state = useHeadless(rest, ref) as RadioGroupState;
  state.layout = layout ?? 'vertical'!;

  useRadioGroupStyles(state, { layout });

  return renderHeadless(state);
});

RadioGroup.displayName = 'RadioGroup';
