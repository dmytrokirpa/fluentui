/**
 * Generated by @fluentui/ds-compiler — do not edit by hand.
 * Recipe: Radio (react-radio/useRadioStyles.styles.ts)
 */
'use client';

import * as React from 'react';
import {
  useRadio as useHeadless,
  renderRadio as renderHeadless,
} from '@fluentui/react-headless-components-preview/radio-group';
import type {
  RadioProps as HeadlessProps,
  RadioState as HeadlessState,
} from '@fluentui/react-headless-components-preview/radio-group';

export type RadioVariants = {};

export type RadioProps = HeadlessProps & RadioVariants;

export type RadioState = HeadlessState & Required<Pick<RadioVariants, never>>;

function useRadioStyles(state: RadioState, variants: RadioVariants): void {
  // no variants

  // no variant attrs

  state.root.className = [state.root.className, 'fui-Radio'].filter(Boolean).join(' ');
  if (state.label) {
    state.label.className = [state.label.className, 'fui-Radio__label'].filter(Boolean).join(' ');
  }
  if (state.input) {
    state.input.className = [state.input.className, 'fui-Radio__input'].filter(Boolean).join(' ');
  }
  if (state.indicator) {
    state.indicator.className = [state.indicator.className, 'fui-Radio__indicator'].filter(Boolean).join(' ');
  }
}

/**
 * Fluent 2 styled Radio built on the headless primitive.
 */
export const Radio = React.forwardRef(function Radio(
  props: RadioProps,
  ref: React.ForwardedRef<HTMLButtonElement | HTMLAnchorElement>,
) {
  const {
    // no variants
    ...rest
  } = props;

  const state = useHeadless(rest, ref) as RadioState;

  useRadioStyles(state, {});

  return renderHeadless(state);
});

Radio.displayName = 'Radio';
