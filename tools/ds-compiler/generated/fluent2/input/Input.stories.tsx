/**
 * Generated by @fluentui/ds-compiler — do not edit by hand.
 */
import type { Meta, StoryObj } from '@storybook/react';
import { Input } from './index';

const meta: Meta<typeof Input> = {
  title: 'Fluent2 CSS/Input',
  component: Input,
  args: {
    appearance: 'outline',
    size: 'medium',
    children: 'Input',
  },
  argTypes: {
    appearance: { control: 'select', options: ['outline', 'underline', 'filled-darker', 'filled-lighter'] },
    size: { control: 'select', options: ['small', 'medium', 'large'] },
  },
};

export default meta;
type Story = StoryObj<typeof Input>;

export const Default: Story = {};

export const Disabled: Story = {
  args: { disabled: true },
};

export const DisabledFocusable: Story = {
  args: { disabled: true, disabledFocusable: true },
};

export const Matrix: Story = {
  render: () => (
    <div style={{ display: 'grid', gap: 12 }}>
      {(['outline', 'underline', 'filled-darker', 'filled-lighter'] as const).map(appearance => (
        <div key={appearance} style={{ display: 'flex', gap: 8, alignItems: 'center', flexWrap: 'wrap' }}>
          <strong style={{ width: 100 }}>appearance={appearance}</strong>
          {(['small', 'medium', 'large'] as const).map(size => (
            <Input key={size} appearance={appearance} size={size}>
              {size}
            </Input>
          ))}
          <Input appearance={appearance} disabled>
            disabled
          </Input>
        </div>
      ))}
    </div>
  ),
};
