/**
 * Generated by @fluentui/ds-compiler — do not edit by hand.
 * Recipe: Button (react-button/useButtonStyles.styles.ts + web-components/button.styles.ts)
 */
'use client';

import * as React from 'react';
import {
  useButton as useHeadless,
  renderButton as renderHeadless,
} from '@fluentui/react-headless-components-preview/button';
import type {
  ButtonProps as HeadlessProps,
  ButtonState as HeadlessState,
} from '@fluentui/react-headless-components-preview/button';

export type ButtonVariants = {
  appearance?: 'secondary' | 'primary' | 'outline' | 'subtle' | 'transparent';
  size?: 'small' | 'medium' | 'large';
  shape?: 'rounded' | 'circular' | 'square';
};

export type ButtonProps = HeadlessProps & ButtonVariants;

export type ButtonState = HeadlessState & Required<Pick<ButtonVariants, 'appearance' | 'size' | 'shape'>>;

function useButtonStyles(state: ButtonState, variants: ButtonVariants): void {
  const appearance = variants.appearance ?? state.appearance;
  const size = variants.size ?? state.size;
  const shape = variants.shape ?? state.shape;

  if (appearance !== undefined) {
    (state.root as Record<string, unknown>)['data-fui-appearance'] = appearance;
  }
  if (size !== undefined) {
    (state.root as Record<string, unknown>)['data-fui-size'] = size;
  }
  if (shape !== undefined) {
    (state.root as Record<string, unknown>)['data-fui-shape'] = shape;
  }

  state.root.className = [state.root.className, 'fui-Button'].filter(Boolean).join(' ');
  if (state.icon) {
    state.icon.className = [state.icon.className, 'fui-Button__icon'].filter(Boolean).join(' ');
  }
}

/**
 * Fluent 2 styled Button built on the headless primitive.
 */
export const Button = React.forwardRef(function Button(
  props: ButtonProps,
  ref: React.ForwardedRef<HTMLButtonElement | HTMLAnchorElement>,
) {
  const { appearance = 'secondary', size = 'medium', shape = 'rounded', ...rest } = props;

  const state = useHeadless(rest, ref) as ButtonState;
  state.appearance = appearance ?? 'secondary'!;
  state.size = size ?? 'medium'!;
  state.shape = shape ?? 'rounded'!;

  useButtonStyles(state, { appearance, size, shape });

  return renderHeadless(state);
});

Button.displayName = 'Button';
