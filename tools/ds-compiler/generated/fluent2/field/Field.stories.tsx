/**
 * Generated by @fluentui/ds-compiler — do not edit by hand.
 */
import type { Meta, StoryObj } from '@storybook/react';
import { Field } from './index';

const meta: Meta<typeof Field> = {
  title: 'Fluent2 CSS/Field',
  component: Field,
  args: {
    orientation: 'vertical',
    size: 'medium',
    children: 'Field',
  },
  argTypes: {
    orientation: { control: 'select', options: ['vertical', 'horizontal'] },
    size: { control: 'select', options: ['small', 'medium', 'large'] },
  },
};

export default meta;
type Story = StoryObj<typeof Field>;

export const Default: Story = {};

export const Disabled: Story = {
  args: { disabled: true },
};

export const DisabledFocusable: Story = {
  args: { disabled: true, disabledFocusable: true },
};

export const Matrix: Story = {
  render: () => (
    <div style={{ display: 'grid', gap: 12 }}>
      {(['vertical', 'horizontal'] as const).map(orientation => (
        <div key={orientation} style={{ display: 'flex', gap: 8, alignItems: 'center', flexWrap: 'wrap' }}>
          <strong style={{ width: 100 }}>orientation={orientation}</strong>
          {(['small', 'medium', 'large'] as const).map(size => (
            <Field key={size} orientation={orientation} size={size}>
              {size}
            </Field>
          ))}
          <Field orientation={orientation} disabled>
            disabled
          </Field>
        </div>
      ))}
    </div>
  ),
};
