/**
 * Generated by @fluentui/ds-compiler — do not edit by hand.
 * Recipe: Switch (react-switch/useSwitchStyles.styles.ts)
 */
'use client';

import * as React from 'react';
import {
  useSwitch as useHeadless,
  renderSwitch as renderHeadless,
} from '@fluentui/react-headless-components-preview/switch';
import type {
  SwitchProps as HeadlessProps,
  SwitchState as HeadlessState,
} from '@fluentui/react-headless-components-preview/switch';

export type SwitchVariants = {
  size?: 'medium' | 'small';
};

export type SwitchProps = HeadlessProps & SwitchVariants;

export type SwitchState = HeadlessState & Required<Pick<SwitchVariants, 'size'>>;

function useSwitchStyles(state: SwitchState, variants: SwitchVariants): void {
  const size = variants.size ?? state.size;

  if (size !== undefined) {
    (state.root as Record<string, unknown>)['data-fui-size'] = size;
  }

  state.root.className = [state.root.className, 'fui-Switch'].filter(Boolean).join(' ');
  if (state.indicator) {
    state.indicator.className = [state.indicator.className, 'fui-Switch__indicator'].filter(Boolean).join(' ');
  }
  if (state.input) {
    state.input.className = [state.input.className, 'fui-Switch__input'].filter(Boolean).join(' ');
  }
  if (state.label) {
    state.label.className = [state.label.className, 'fui-Switch__label'].filter(Boolean).join(' ');
  }
}

/**
 * Fluent 2 styled Switch built on the headless primitive.
 */
export const Switch = React.forwardRef(function Switch(
  props: SwitchProps,
  ref: React.ForwardedRef<HTMLButtonElement | HTMLAnchorElement>,
) {
  const { size = 'medium', ...rest } = props;

  const state = useHeadless(rest, ref) as SwitchState;
  state.size = size ?? 'medium'!;

  useSwitchStyles(state, { size });

  return renderHeadless(state);
});

Switch.displayName = 'Switch';
